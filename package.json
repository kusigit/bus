{
  "name": "bus",
  "version": "0.0.1",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "amqplib": "0.5.6",
    "logger": "github:kusigit/logger"
  }
}
